"""
Application configuration for the subscriptions application.
Sets up the metadata for the component managing reader interest and access to content.
"""
from django.apps import AppConfig


class SubscriptionsConfig(AppConfig):
    """
    Configuration for the subscriptions app.
    """
    name = 'subscriptions'
